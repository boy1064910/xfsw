server:
  port: 8021
  
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/boy1064910/config.git/     # 配置git仓库的地址
          #search-paths: xfsw-config-server,xfsw-web-fe-entry
          username: boy1064910                                        # git仓库的账号
          password: pick1987                                    # git仓库的密码
eureka:
  client:
    serviceUrl:
      defaultZone: http://${security.user.name}:${security.user.password}@localhost:8001/eureka/   ## 注册中心eurka地址
      
security:   
  user:  
    name: xfsw #eureka 用户名  
    password: xfsw-eureka #eureka 密码  