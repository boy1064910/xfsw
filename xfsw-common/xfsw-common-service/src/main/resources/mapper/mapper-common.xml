<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Common">
	<insert id="delBakOperation" parameterType="java.util.Map">
  		${sql}
	</insert>
	
	<delete id="delOperation" parameterType="java.util.Map">
		${sql}
	</delete>
	
	<update id="updateOperation" parameterType="java.util.Map">
		${sql}
	</update>
	
	<select id="queryOperation" parameterType="java.util.Map" resultType="java.util.Map">
		${sql}
	</select>
	
	<insert id="insertOperation" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="id">
		${sql}
	</insert>
	
	<select id="getCount" parameterType="java.util.Map" resultType="java.lang.Integer">
		${sql}
	</select>
</mapper>